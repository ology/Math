#!/usr/bin/perl
use strict;
use warnings;
die "Euclid greatist common divisor\nUsage: perl $0 x y\n"
    unless @ARGV == 2;
print gcd(@ARGV);
sub gcd {
    my ( $m, $k ) = @_;
    return $k == 0 ? $m : gcd($k, $m % $k);
}
